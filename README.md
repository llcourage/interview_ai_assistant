# 🔥 AI 面试助手 (Windows 桌面版)

一个强大的 Windows 桌面应用，帮助你在面试中快速获得 AI 辅助。

## ✨ 主要功能

- **🎯 全局快捷键**
  - `Ctrl+H`: 全屏截图
  - `Ctrl+Enter`: 将截图发送给 AI 分析
  - `Ctrl+B`: 显示/隐藏悬浮窗

- **🎨 悬浮窗界面**
  - 永远置顶 (Always-on-top)
  - 50% 透明度，不遮挡视线
  - 置顶在屏幕最上方，不遮挡中间内容
  - 默认只显示快捷键，按需展开内容
  - 智能调整高度（最大屏幕高度 50%）
  - 可通过 Ctrl+B 快速隐藏/显示

- **🤖 AI 视觉分析**
  - 使用 OpenAI Vision API (GPT-4o)
  - 自动识别面试问题
  - 提供详细解答和代码示例

- **💻 本地后端服务**
  - FastAPI 高性能后端
  - 完整的 API 文档
  - 支持自定义 OpenAI 兼容 API

## 🏗️ 技术架构

### 前端
- **Electron** - 桌面应用框架
- **React** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 快速构建工具

### 后端
- **FastAPI** - 高性能 Python Web 框架
- **OpenAI API** - 多模态 AI 分析
- **Uvicorn** - ASGI 服务器

## 📦 快速开始

### 前置要求

- **Node.js** 18+ 和 npm
- **Python** 3.8+
- **OpenAI API Key** (或兼容的 API)

### 1. 安装依赖

运行自动安装脚本：

```bash
install.bat
```

或手动安装：

```bash
# 前端依赖
npm install

# 后端依赖
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

### 2. 配置 API Key

编辑 `backend\.env` 文件：

```env
OPENAI_API_KEY=sk-your-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o
```

### 3. 启动应用

**一键启动（推荐）：**

```bash
start-all.bat
```

**分别启动：**

```bash
# 终端 1 - 启动后端
start-backend.bat

# 终端 2 - 启动前端
start-frontend.bat
```

## 🎮 使用说明

1. **启动应用后，会看到两个窗口：**
   - 主窗口：显示状态和详细信息
   - 悬浮窗：右上角的小窗口，永远置顶

2. **截图：**
   - 按 `Ctrl+H` 进行全屏截图
   - 截图会显示在悬浮窗中

3. **AI 分析：**
   - 按 `Ctrl+Enter` 发送截图给 AI
   - 等待几秒，AI 回复会显示在悬浮窗和主窗口中

4. **查看结果：**
   - 悬浮窗显示简短回复
   - 主窗口显示完整回复

## 📱 界面预览

### 主界面
- 显示快捷键说明
- 显示当前状态
- 显示截图预览
- 显示完整的 AI 回复

### 悬浮窗
- 紧凑的界面设计
- 截图缩略图
- AI 回复摘要
- 快捷键提示

## 🔧 开发

### 项目结构

```
AI-Interview-Assistant/
├── electron/              # Electron 主进程
│   ├── main.js           # 主进程入口
│   └── preload.js        # 预加载脚本
├── src/                  # React 前端
│   ├── main.tsx          # 前端入口
│   ├── App.tsx           # 主界面
│   ├── Overlay.tsx       # 悬浮窗界面
│   └── *.css             # 样式文件
├── backend/              # FastAPI 后端
│   ├── main.py           # 后端入口
│   ├── vision.py         # 视觉分析模块
│   ├── start.py          # 启动脚本
│   └── requirements.txt  # Python 依赖
├── package.json          # Node.js 配置
├── vite.config.ts        # Vite 配置
└── *.bat                 # Windows 批处理脚本
```

### 开发命令

```bash
# 开发模式
npm run dev

# 构建前端
npm run build

# 打包 Electron
npm run package

# 启动后端
cd backend
python start.py
```

### 调试

- **前端调试**: Electron 窗口会自动打开 DevTools
- **后端调试**: 访问 http://127.0.0.1:8000/docs 查看 API 文档
- **日志**: 查看终端输出

## 📦 打包发布

构建 Windows 安装包：

```bash
build.bat
```

或手动执行：

```bash
npm run build
npm run build:electron
```

生成的安装包在 `dist-electron/` 目录下。

## 🔐 安全说明

- API Key 存储在本地 `.env` 文件中，不会上传到云端
- 截图数据仅发送到你配置的 API 端点
- 建议使用自己的 OpenAI API Key

## 🐛 常见问题

### 1. 快捷键不工作

- 检查是否有其他程序占用了相同快捷键
- 确保应用有足够的系统权限

### 2. 无法连接后端

- 确保后端服务正在运行 (http://127.0.0.1:8000)
- 检查防火墙设置
- 查看后端终端的错误信息

### 3. API 调用失败

- 检查 `backend\.env` 中的 API Key 是否正确
- 确认 API 账户有足够的余额
- 检查网络连接

### 4. 截图失败

- 确保应用有屏幕录制权限
- Windows 设置 → 隐私 → 屏幕录制

## 🌟 高级配置

### 使用其他 AI 服务

编辑 `backend\.env`：

```env
# 使用 Azure OpenAI
OPENAI_BASE_URL=https://your-resource.openai.azure.com/
OPENAI_MODEL=gpt-4-vision

# 使用其他兼容 OpenAI API 的服务
OPENAI_BASE_URL=https://api.your-provider.com/v1
OPENAI_MODEL=your-model-name
```

### 修改快捷键

编辑 `electron/main.js`：

```javascript
// 修改截图快捷键
globalShortcut.register('CommandOrControl+Shift+S', async () => {
  await captureScreen();
});
```

### 调整悬浮窗大小和位置

编辑 `electron/main.js` 中的 `createOverlayWindow()` 函数。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

## 🙏 致谢

- OpenAI - 提供强大的 Vision API
- Electron - 跨平台桌面应用框架
- FastAPI - 现代 Python Web 框架

---

**⚡ 快速链接**

- [OpenAI API 文档](https://platform.openai.com/docs)
- [Electron 文档](https://www.electronjs.org/)
- [FastAPI 文档](https://fastapi.tiangolo.com/)

**💡 提示**

这个项目适合用于学习和个人使用。请遵守相关考试和面试的规则和道德准则。

