# Root BUILD file for Interview Assistant project

package(default_visibility = ["//visibility:public"])

# This is a minimal BUILD file to ensure the workspace can be built
# More specific BUILD files can be added to subdirectories as needed

